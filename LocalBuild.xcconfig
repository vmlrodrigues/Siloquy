// LocalBuild.xcconfig
// Build configuration for local/unsigned builds (no Apple Developer certificate required).
// Used by `make local` — does NOT affect Debug or Release configurations.

// Ad-hoc signing (works without a developer account)
CODE_SIGN_IDENTITY = -
CODE_SIGN_STYLE = Manual
DEVELOPMENT_TEAM =
PROVISIONING_PROFILE_SPECIFIER =

// Entitlements are set via absolute path in the Makefile command line
// (relative paths break SPM package targets that resolve from their own source root)

// Add LOCAL_BUILD compilation flag so Swift code can use #if LOCAL_BUILD
SWIFT_ACTIVE_COMPILATION_CONDITIONS = $(inherited) LOCAL_BUILD

// Apple Silicon only — Intel is not supported
ARCHS = arm64
VALID_ARCHS = arm64

// Separate bundle ID so the dev build gets its own permissions slot in
// System Settings and never interferes with the released app's permissions.
// iCloud/CloudKit is intentionally absent on the dev build.
PRODUCT_BUNDLE_IDENTIFIER = com.victorrodrigues.siloquy.dev
INFOPLIST_KEY_CFBundleDisplayName = Siloquy Dev
