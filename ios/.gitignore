# The Xcode project and per-target Info.plists are generated from
# project.yml by XcodeGen. Regenerate with:  cd ios && xcodegen generate
Siloquy.xcodeproj/
Siloquy/Info.plist
SiloquyWidget/Info.plist

# Xcode local artifacts
xcuserdata/
*.xcuserstate
.DS_Store
build/

# TestFlight upload secrets (the .p8 lives outside the repo)
testflight.config

# Benchmark corpus — real dictation, never committed. Built by pulling the app's
# store off the phone; the target falls back to synthetic samples without it.
SiloquyBench/Fixtures/
